.DS_Store
build/
dist/
# Launch/marketing assets — kept locally, not part of the shipped project
promo/
# Generated by make_icon.swift / build.sh
AppIcon.iconset/
AppIcon.icns
CLAUDE.md

# Apple signing / notarization keys — never commit
# (base64 these into the GitHub Actions secrets instead)
*.p8
*.p12
