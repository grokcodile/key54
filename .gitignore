.DS_Store
build/
dist/

# Junk drawer: anything kept next to the project but not part of it (the launch
# and marketing assets live in junk/promo). Ignored wholesale, so nothing
# dropped in here needs a rule of its own.
junk/

# Generated by make_icon.swift / build.sh
AppIcon.iconset/
AppIcon.icns
CLAUDE.md

# Apple signing / notarization keys — never commit
# (base64 these into the GitHub Actions secrets instead)
*.p8
*.p12
